---
# Istio (mode sidecar) + Kiali via Helm : le service mesh porte mTLS, identité
# (SPIFFE dérivée du ServiceAccount) et autorisation par route.
- name: Repo Helm Istio
  ansible.builtin.command: helm repo add istio {{ istio_helm_repo }} --force-update
  changed_when: false
  environment: { KUBECONFIG: /etc/rancher/k3s/k3s.yaml }

- name: Repo Helm Kiali
  ansible.builtin.command: helm repo add kiali {{ kiali_helm_repo }} --force-update
  changed_when: false
  environment: { KUBECONFIG: /etc/rancher/k3s/k3s.yaml }

- name: helm repo update
  ansible.builtin.command: helm repo update istio kiali
  changed_when: false
  environment: { KUBECONFIG: /etc/rancher/k3s/k3s.yaml }

- name: Namespace istio-system
  ansible.builtin.shell:
    cmd: |
      k3s kubectl apply -f - <<'EOF'
      apiVersion: v1
      kind: Namespace
      metadata:
        name: {{ istio_namespace }}
      EOF
  changed_when: true

# base (CRDs) + istiod (control plane). Pas de gateway Istio : on garde Traefik.
- name: Istio base (CRDs)
  ansible.builtin.command: >-
    helm upgrade --install istio-base istio/base
    --version {{ istio_chart_version }}
    --namespace {{ istio_namespace }}
    --set defaultRevision=default
  environment: { KUBECONFIG: /etc/rancher/k3s/k3s.yaml }
  changed_when: true

- name: Istiod (control plane)
  ansible.builtin.command: >-
    helm upgrade --install istiod istio/istiod
    --version {{ istio_chart_version }}
    --namespace {{ istio_namespace }}
    --wait --timeout 5m
  environment: { KUBECONFIG: /etc/rancher/k3s/k3s.yaml }
  changed_when: true

# Kiali : dashboard du mesh, branché sur le Prometheus de la stack observability.
- name: Values Kiali
  ansible.builtin.template:
    src: kiali-values.yaml.j2
    dest: /tmp/kiali-values.yaml
    mode: "0644"

- name: Kiali (dashboard du mesh)
  ansible.builtin.command: >-
    helm upgrade --install kiali-server kiali/kiali-server
    --version {{ kiali_chart_version }}
    --namespace {{ istio_namespace }}
    --values /tmp/kiali-values.yaml
    --wait --timeout 5m
  environment: { KUBECONFIG: /etc/rancher/k3s/k3s.yaml }
  changed_when: true
